---
description: Add or review restrained SVG grain on a web interface surface
argument-hint: "[surface, route, or file]"
---
Load and follow the `craft-noise` skill. Apply its decision process to ${ARGUMENTS:-the current interface change}. If no target can be inferred, ask one focused question before editing.
