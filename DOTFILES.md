# DOTFILES

**Generated:** 2026-09-05T04:20:27.093Z
**Commit:** d8f9250

macOS dev env via GNU Stow, managed by `jdot`.

## STRUCTURE

```
dotfiles/
├── jdot
├── folders.toml         # Folder jump aliases + Finder aliases
├── .jdotignore          # Machine-local package opt-outs (gitignored)
├── home/
│   ├── agents/
│   ├── eza/
│   ├── fastfetch/
│   ├── ghostty/
│   ├── git/
│   ├── herdr/
│   ├── mise/
│   ├── pi/
│   ├── starship/
│   ├── tmux/
│   ├── vscode/
│   ├── zed/
│   └── zsh/
└── packages/bundle     # Brewfile
```

## COMMANDS

```bash
jdot stow              # Stow all packages (skips .jdotignore entries)
jdot unstow [pkg]      # Unstow one package, or everything
jdot alias             # Regenerate shell + Finder aliases from folders.toml
jdot doctor            # Find broken symlinks under $HOME
jdot benchmark-shell   # zsh startup perf (-r N runs, -v verbose)
jdot digest             # Regenerate this file
jdot pi-digest          # Regenerate home/pi/PI.md
jdot skills-check       # Validate repository skills without changes
```

## FOLDER ALIASES

`gotogit`, `dotfiles`

## ANTI-PATTERNS

- Edit an unstowed local copy and expect it to update this repository; verify the symlink target first
- Hand-edit `~/.config/jdot/aliases.zsh` (regenerated by `jdot alias`, not tracked)
- Hardcode paths (use `$HOME`, not `/Users/justin`)
- Commit `.jdotignore` (machine-local; edit `.jdotignore.example` for shared defaults)
